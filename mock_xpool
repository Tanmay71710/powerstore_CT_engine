#!/bin/bash
# Mock xpool script for testing purposes

# Parse arguments
command=$1
shift

if [ "$command" = "list" ]; then
    # Mock cluster list output in expected format
    # Format: [['cluster1', 'cluster2'], ['cluster3']]
    echo "[['mock-cluster-1', 'mock-cluster-2'], ['mock-cluster-3']]"
else
    echo "Unknown command: $command"
    exit 1
fi
